include components

doctype html
html
    head
        title #{process.env.APP_NAME}
        meta(charset="utf-8")
        link(rel='stylesheet', href='/styles/main.css')

    body
        div.content-origin
            +header(itemsNavBar)
            block layout-content
