
mixin header(items)
    header
        nav.nav-header
            ul
                li
                    h2.nav-title #{process.env.appname}
                if items
                    for item in items
                        li
                            a(href=item.href) #{item.label}